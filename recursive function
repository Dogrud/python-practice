import math
import time
def usalma(a,b):
    print(math.pow(a,b))
    start =time.time
    time.sleep(1)
    print(math.pow(a,b))

    finish = time.time
    print("fonksiyon " +(finish-start)+" saniye sürdü.")

def faktoriyel(num):
    print(math.factorial(num))

usalma(2,3)
